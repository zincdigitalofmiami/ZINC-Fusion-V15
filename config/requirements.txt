# ZINC-FUSION-V15 — Minimal runtime dependencies
#
# AUTHORITATIVE SOURCE: pyproject.toml + uv.lock
# This file exists for tooling compatibility only (e.g. preflight_check.py).
# For the full dependency tree, use: uv sync
#
# Database (required at minimum for any Python process that touches Postgres)
psycopg2-binary
sqlalchemy>=2.0
