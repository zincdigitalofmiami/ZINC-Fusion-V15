# NOTE: This repository runs on Dagster local by default.
# This file is kept as legacy/reference-only and is NOT required for `dagster dev`.
# Local configuration is driven by `pyproject.toml` ([tool.dagster]) and the module
# `src/quickstart_etl/definitions.py`.
#
# If you later choose to use Dagster Cloud, confirm the expected workspace spec and
# update this file intentionally.

locations:
  - location_name: zinc_fusion_v15
    code_source:
      module_name: quickstart_etl.definitions
    # The Python package lives under `src/`, but the working directory should be the repo root.
    working_directory: .
