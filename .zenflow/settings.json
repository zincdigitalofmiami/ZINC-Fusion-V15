{
  "setup_script": "python3 -m venv .venv && .venv/bin/pip install -r config/requirements.txt -r config/requirements.api.txt && cd frontend && npm install && cd ../config && npm install && cd ../scripts && npm install",
  "verification_script": ".venv/bin/pytest -q && cd frontend && npm run lint",
  "copy_files": [".env"]
}
